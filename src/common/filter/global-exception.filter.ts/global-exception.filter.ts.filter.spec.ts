import { GlobalExceptionFilterTsFilter } from './global-exception.filter.ts.filter';

describe('GlobalExceptionFilterTsFilter', () => {
  it('should be defined', () => {
    expect(new GlobalExceptionFilterTsFilter()).toBeDefined();
  });
});
